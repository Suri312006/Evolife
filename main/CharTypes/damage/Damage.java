package main.CharTypes.damage;

import main.CharTypes.Base;

public class Damage extends Base {

    public Damage(double health, double AD, double AP, double mana, double armor, boolean alive) {
        super(health, AD, AP, mana, armor, alive);
    }
}